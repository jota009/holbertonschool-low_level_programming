#include <stdio.h>

/**
 * main - Printing single digits in base form
 * Return: Value of base 10 digits
 */

int main(void)
{
	int s;

	for (s = 0; s < 10; s++)
	{
		printf("%d", s);
	}
	printf("\n");

	return (0);
}
