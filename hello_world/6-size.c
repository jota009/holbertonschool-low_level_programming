#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	size_t size_of_int = sizeof(int);

	printf("Size of char: %zu byte(s)\n", size_of_char);
	return (0);
}
